# Custom app compatibility

Android WebView now leaves sub-frame navigation inside imported custom apps instead of opening the system browser. This build also contains the global back and visible system-bar fixes.
